# 設計図3Dビルダー — ラフ画・図面から素材と図面を構築する

`設計図3Dビルダー.html` は、**設計図やラフ画(手描きスケッチ・平面図・立面図・写真)を渡すと、
Claude が解析して次の4つを構築する**単一ファイルのブラウザアプリです。

| 出力 | 内容 |
|---|---|
| **仕様JSON** | 寸法(m)・部位分解・配色・質感の構造化データ |
| **平面図** | 1mグリッド・寸法線・部位ラベル付きの整形済み図面 (PNG保存可) |
| **3Dモデル** | Three.js r128 によるプレビュー (回転・拡大縮小可) |
| **GLB / コード** | GLB書き出し (Blender等で利用可) + 本体ゲームに移植できる `buildModel(THREE, group)` 関数 |

生成される Three.js コードは本体 `寝殿造り3D探訪_統合版.html` と同じ **r128 API・旧カラーマネジメント**
(`SRGBColorSpace` 等の新API禁止)・**外部アセット不使用**の規約で出力されるため、
そのままゲームの造形ヘルパー群の隣に移植できます。

## 使い方

1. `設計図3Dビルダー.html` をブラウザで開く (Three.js は CDN 読み込みのため**要ネット接続**)
2. 右上に Anthropic APIキー (`sk-ant-...`) を入力 — この端末の localStorage にのみ保存されます
3. 画像をクリック選択 / ドラッグ&ドロップ / Ctrl+V 貼り付けでセット
4. 必要なら補足指示 (「幅12m」「檜皮葺き」など) を書いて **構築開始**
5. 生成後は「修正指示」で対話的に手直し、「GLB書き出し」で素材として保存

## モデルフォールバック設計 — Fable が使えなくても同レベルを出す仕組み

「Fable 5 が使えなくなっても Opus / Sonnet で同水準の出力になる」ことを、
プロンプトの気合いではなく**構造**で担保しています。

### 1. モデル連鎖の自動カスケード
`Fable 5 → Opus 4.8 → Sonnet 5` の順に試行し、以下のすべてのケースで自動的に次のモデルへ移ります。

- モデルが存在しない / アクセス権がない (404 / 403)
- レート制限・過負荷 (429 / 529) やサーバーエラー (500)
- 安全機構による応答辞退 (`stop_reason: refusal`)

さらに Fable 5 のテキスト生成リクエストには**サーバー側フォールバック**
(`fallbacks: [{model: "claude-opus-4-8"}]`, beta `server-side-fallback-2026-06-01`) を併用しており、
辞退時は同一呼び出し内で Opus 4.8 が透過的に引き継ぎます。
ヘッダーのモデル選択で優先モデルを固定しても、失敗時は残りのモデルへフォールバックします。

### 2. 構造化出力 (JSONスキーマ強制)
解析結果 (Stage 1) は `output_config.format` の JSON スキーマで**出力形式を強制**します。
どのモデルが応答しても、寸法・部位・配色のフィールド構成は完全に同一です。

### 3. 決定的レンダラ (モデル差が出ない層)
平面図と「仕様ベース3D」は、仕様JSONから**アプリ側のコードが機械的に描画**します。
この層の品質はモデルに一切依存しません。詳細モデル生成が失敗しても、
平面図・3Dプレビュー・GLB書き出しは常に成立します。

### 4. 自動修復ループ (弱いモデルの失敗を吸収)
Stage 2 の生成コードは、その場で実行検証 (関数定義チェック → 構築 → 1フレーム描画) し、
エラーが出たらエラーメッセージを添えて**最大2回まで自動で修正依頼**します。
モデルの一発成功率の差を、このループが均します。

### モデル別パラメータの自動調整

| モデル | thinking | effort |
|---|---|---|
| Fable 5 | パラメータ省略 (常時ON仕様のため明示指定は不可) | high |
| Opus 4.8 | `{type: "adaptive"}` | high |
| Sonnet 5 | `{type: "adaptive"}` | high |

## パイプライン

```
画像 (長辺2048pxに自動縮小)
  │
  ├ Stage 1: 解析 ──→ 仕様JSON (スキーマ強制)
  │                     ├→ 平面図 (決定的描画)
  │                     └→ 仕様ベース3D (決定的構築) ← 保険レイヤ
  │
  └ Stage 2: コード生成 (画像 + 仕様JSON)
        └→ 実行検証 → NG → 自動修復 (最大2回)
             └→ OK → 詳細3Dに差し替え → GLB書き出し / 修正指示ループ
```

## 注意事項

- APIキーはブラウザの localStorage に平文保存されます。**共用端末では使わない**でください。
- API はブラウザから直接呼び出します (`anthropic-dangerous-direct-browser-access` ヘッダー)。
  配布する場合はキーを各自で入力してもらう前提の設計です。
- 本体ゲームと違い開発支援ツールのため、Three.js r128 / OrbitControls / GLTFExporter を
  jsDelivr CDN から読み込みます (バージョンは本体と同じ r128 に固定)。
