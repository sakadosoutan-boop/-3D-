{
  "name": "shinden-zukuri-3d",
  "private": true,
  "scripts": {
    "build:story": "node scripts/build-story.js",
    "build:story:check": "node scripts/build-story.js --check",
    "verify:html": "node scripts/verify-html.js",
    "verify:story": "node scripts/verify-story.js",
    "verify:routes": "node scripts/verify-story-routes.js",
    "smoke": "node scripts/smoke-playwright.js",
    "test": "npm run build:story:check && npm run verify:html && npm run verify:story && npm run verify:routes && npm run smoke"
  },
  "devDependencies": {
    "playwright": "1.61.1"
  }
}
