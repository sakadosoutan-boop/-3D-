{
  "name": "shinden-zukuri-3d",
  "private": true,
  "scripts": {
    "verify:html": "node scripts/verify-html.js",
    "smoke": "node scripts/smoke-playwright.js",
    "preflight": "node scripts/preflight.js",
    "preflight:bench": "node scripts/preflight.js --bench",
    "test": "npm run verify:html && npm run smoke"
  },
  "devDependencies": {
    "playwright": "1.61.1"
  }
}
